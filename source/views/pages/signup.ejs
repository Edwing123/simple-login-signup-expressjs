<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/_head-common") %>
    
    <!-- metadata information -->
    <meta name="description" content="Create an account" />
    <meta name="keywords" content="create account, username and password" />

    <!-- title -->
    <title>Signup</title>
  </head>
  <body>
    <div class="container" id="app">
      <%- include("../partials/_header") %>
      <main class="main">
        <section class="section">
          <h2 class="sectionTitle">Create an account</h2>
          <%- include("../partials/_form", { forLogin: false }) %>
          <% if (registered) { %>
            You are already registered
          <% } %>
        </section>
      </main>
      <%- include("../partials/_footer") %>
    </div>
    <!-- scripts -->
  </body>
</html>
